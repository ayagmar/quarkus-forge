= Release Notes - Refactor Track (2026-02)
:toc:

== Behavior Changes

* No intentional public CLI contract changes were introduced in this refactor track.
* Internal structure was decomposed to improve maintainability and testability while preserving behavior parity.
* Documentation coverage expanded with generated AsciiDoc modules and landing navigation shell.

== Migration Notes

* No operator data migration is required.
* Persistence locations remain unchanged:
  ** `~/.quarkus-forge/preferences.json`
  ** `~/.quarkus-forge/favorites.json`
  ** `~/.quarkus-forge/catalog-snapshot.json`
* Existing command usage for `quarkus-forge` and `quarkus-forge generate` remains valid.

== Operator Impact Summary

* Positive: clearer docs, explicit keybinding/theming guidance, and stronger architecture governance artifacts.
* Neutral: exit-code map, selection semantics, provider roots, and fallback contracts remain stable.
* Risk posture: archive extraction remains custom by decision record, backed by safety and property-style test coverage.

== Validation Evidence

[source,bash]
----
mvn -q -Pdocs generate-resources
mvn -q -DskipITs test
mvn -q spotless:check checkstyle:check
----
