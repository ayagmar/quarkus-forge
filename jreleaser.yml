project:
  name: quarkus-forge
  authors:
    - ayagmar
  versionPattern: CUSTOM
  stereotype: CLI
  description: Keyboard-first Quarkus project generator TUI
  copyright: 2026 ayagmar
  links:
    homepage: https://github.com/ayagmar/quarkus-forge
  license: MIT

release:
  github:
    owner: ayagmar
    name: quarkus-forge
    branch: master
    skipTag: true
    overwrite: true
    artifacts: true
    changelog:
      enabled: true
      formatted: ALWAYS
      preset: conventional-commits
      sort: DESC
      skipMergeCommits: true
      links: true
      format: "- {{commitShortHash}} {{commitTitle}}"
      categoryTitleFormat: "### {{categoryTitle}}"
      contributorsTitleFormat: "### Contributors"

checksum:
  individual: true

signing:
  active: NEVER

distributions:
  quarkus-forge:
    type: BINARY
    artifacts:
      - path: artifacts/quarkus-forge-jvm.jar
      - path: artifacts/quarkus-forge-linux-x86_64
        platform: linux
      - path: artifacts/quarkus-forge-macos-aarch64
        platform: osx
      - path: artifacts/quarkus-forge-windows-x86_64.exe
        platform: windows
      - path: artifacts/jbang-catalog.json

  quarkus-forge-headless:
    type: BINARY
    artifacts:
      - path: artifacts/quarkus-forge-headless.jar
      - path: artifacts/quarkus-forge-headless-linux-x86_64
        platform: linux
      - path: artifacts/quarkus-forge-headless-macos-aarch64
        platform: osx
      - path: artifacts/quarkus-forge-headless-windows-x86_64.exe
        platform: windows
