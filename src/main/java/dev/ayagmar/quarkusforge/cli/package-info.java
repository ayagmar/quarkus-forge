/**
 * CLI entry points and Picocli command models.
 *
 * <p>This package owns argument parsing and exit surfaces, while delegating request assembly,
 * runtime wiring, and generation behavior to narrower packages.
 */
package dev.ayagmar.quarkusforge.cli;
