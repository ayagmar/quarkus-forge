/**
 * Forgefile models and persistence.
 *
 * <p>This package owns the serialized recipe and lock representation and should stay independent of
 * CLI parsing and TUI state.
 */
package dev.ayagmar.quarkusforge.forge;
