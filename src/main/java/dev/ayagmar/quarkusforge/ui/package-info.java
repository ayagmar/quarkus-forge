/**
 * Interactive TUI state machine, rendering, and async coordination.
 *
 * <p>This package owns Tamboui-facing UI behavior for the interactive flow. It may depend on shared
 * domain, API, forge, persistence, and post-generation abstractions, but it should not own runtime
 * assembly or transport concerns.
 */
package dev.ayagmar.quarkusforge.ui;
