/**
 * Shared orchestration helpers between CLI and headless flows.
 *
 * <p>This package converts boundary inputs into domain requests and owns the reusable startup-state
 * contract without depending on Picocli or the TUI.
 */
package dev.ayagmar.quarkusforge.application;
