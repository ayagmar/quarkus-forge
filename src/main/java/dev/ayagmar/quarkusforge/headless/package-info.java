/**
 * Non-interactive generation flow and headless boundary adapters.
 *
 * <p>This package may depend on application, domain, API, persistence, forge code, and the small
 * CLI command-model seam used for headless orchestration, but it must remain independent from
 * runtime assembly, TUI state, and Tamboui.
 */
package dev.ayagmar.quarkusforge.headless;
