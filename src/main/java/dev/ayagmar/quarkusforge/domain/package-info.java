/**
 * Core request models, defaults, and validation rules shared across flows.
 *
 * <p>This package defines the stable domain objects used by both interactive and headless paths. It
 * must remain independent of transport, persistence, runtime wiring, and Tamboui.
 */
package dev.ayagmar.quarkusforge.domain;
